# Netlify redirects file for SPA routing
# This ensures all routes are handled by React Router on the client side
# The /* /index.html 200 rule redirects all paths to index.html with a 200 status code
# This allows React Router to handle client-side routing

/*    /index.html   200

